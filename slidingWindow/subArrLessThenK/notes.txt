Subarray Product Less Than K
Given an array of positive integers nums and an integer k, return the number of contiguous subarrays where the product of all the elements in the subarray is strictly less than k.
Input: nums = [10,5,2,6], k = 100
Output: 8
Explanation: The 8 subarrays that have product less than 100 are: [10], [5], [2], [6], [10,5], [5,2], [2,6], [5,2,6]

notes:- 
there is one formula (right_pointer-left_pointer+1) is giving the valid no of contiguous subarray ends with right_pointer value
 inside that range example
 [1,2,3] lp=0 rp=2 count=2-0+1=3 how three so [3,[2,3],[1,2,3]] like this so hwo you are saying subarray valid

 so in this question saying if product is < 100 than valid so if pdt of array is valid than all possible subarray are also 
 valid right example target is k=8  pdt should be less then k
 if this array [1,2,3] pdt is 7 < k so all possible pdt is less then k you can check like take any sub array 2*3=6 1*3=3 all

 so i am saying if we find all valid subarray at index right from starting to end we can find out total no of valid subarray 
 so we start right index from 0 to n-1 and find all valid subarray at all right index and sub it that is answer
 