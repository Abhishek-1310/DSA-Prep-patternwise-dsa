Subarray Sum Equals K
Given an array of integers nums and an integer k, return the total number of continuous subarrays whose sum equals to k.
nums = [1,2,3], k = 3
Output:2
Explanation: The subarrays [3] and [1,2] sum to 3.

notes
how to make map=> let map = new Map() => map.get(key) => map.set(key,value);
how to increase count of key=>  map.set(key, (map.get(key)??0)+1)

index   0   1   2   3
nums    1   2   3   4      index 1 & 3 sum is(2+3+4) = 9 see how it comes with formula
prefix  1   3   6   10     sum(1 to 3) = prefix[3]-prefix[1-1] = prefix[3]-prefix[0] = 10-1=9

sum from i to j formula = sum(i..j) = prefix[j] - prefix[i-1] => when k=sum(i..j) then k=prefix[j] - prefix[i-1]
  [ prefix[i-1] = prefix[j] - k then we check the hashmap ] if it present then we increse count
(Have we seen (currentPrefix - k) before? in map then count+ occurence of that prefix)

why we should set prefix sum count after check arr=[0] k=0      prefixsum-k=val present in map 
map = {0:1}    if you set first so count 0:2 then you will check 0-0=0  count+=map.get(0) count+=2 but count should be 1
psum = 0
count = 0
