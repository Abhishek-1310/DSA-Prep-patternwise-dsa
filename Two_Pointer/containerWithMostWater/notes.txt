>>> how to find width of walls

    >> always do rightpointer - leftpointer to find width
    >> and right inside while loop so that width is updated every time pointers are moved

>>>> container with most water (values are line not bar)

You are given an array of positive integers where each integer represents the height of a vertical line drawn at that index.
The goal is to find two lines that, together with the x - axis, form a container that can hold the maximum amount of water.

>> always make maxWater variable to store maximum water found so far.
>> Initialize two pointers, one at the start(left) and one at the end(right) of the array.

[1, 5, 4, 3] min of 1 and 3 is 1(width is 3 - 0=3) area("maxWater") is 3 * 1=3

>> then find currentwater everytime and comapre with maxWater if greater update maxWater
>> and update left pointer and right pointer when moving pointers always move the pointer which is smaller in height
 because moving the larger height pointer will not help in finding a larger area as area is limited by the smaller height.