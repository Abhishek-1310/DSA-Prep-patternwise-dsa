Two Sum
Given an array of integers nums and an integer target, return indices of the two numbers such that they add up to target.
You may assume that each input would have exactly one solution, and you may not use the same element twice.
You can return the answer in any order.

Examples:
Input: nums = [2,7,11,15], target = 9 Output:[0,1]

Two Sum
-> find two diff index such that there values add up to the target value 
 
> arr.length can give length > arr.sort() will not work arr.sort((a,b)=>a-b) will sort array
> arr.map().sort() can do more then one operation > arr is reserved word don't use as variable
⦁	arr.map((value,index)=>({value,index})) to preseve index of original array 
⦁	then in sort (a,b) => a.value -a.value you have to do 

logic
if sum > target => right pointer move right--
if sum <  target => left pointer move left++
if sum === target return index of orignal array not sorted array

why we shift right pointer when sum is greater :- arr is in ascending order if sum is > target we need lower value
so we shift right pointer to left side to reduce value same as if sum is < target we need higher value so shift left 
pointer to right side for increase the value 

if sum is equal to target shift both left and right pointer so we can't use same value twice and going further to capture 
other sum.

using map
iterate through array if target is 7 and 1st element 3 so 7-3 = 4 if 4 is present in map then return index or else add
that value in map

use map instead of two pointer is when you have to return index

