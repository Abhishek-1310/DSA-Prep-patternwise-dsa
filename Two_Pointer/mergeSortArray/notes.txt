Merge Sorted Array
You are given two integer arrays nums1 and nums2, sorted in non-decreasing order, and two integers m and n, representing
the number of elements in nums1 and nums2 respectively. Merge nums1 and nums2 into a single array sorted in non-decreasing order.
The final sorted array should not be returned by the function, but instead be stored inside the array nums1. To accommodate this, 
nums1 has a length of m + n, where the first m elements denote the elements that should be merged, and the last n elements are set
to 0 and should be ignored. nums2 has a length of n.

Input: nums1 = [1,2,3,0,0,0], m = 3, nums2 = [2,5,6], n = 3 Output:[1,2,2,3,5,6]
Explanation: After merging, nums1 becomes [1,2,2,3,5,6].

Extract both arrays and their valid lengths from the object (nums1, m, nums2, n).
Use three pointers starting from the end of both arrays to avoid overwriting values.
Compare the last valid elements of nums1 and nums2 and place the larger one at the end of nums1.
Move pointers backward after each placement until one array is exhausted.
If elements remain in nums2, copy them into nums1, because remaining elements in nums1 are already in correct order.

let { nums1, m, nums2, n } = nums;

when arr1 element exhausted but arr2(nums2) value untouched arr2 pointer is j
while (j >= 0) {
        nums1[k--] = nums2[j--];
}