Largest Perimeter Triangle
Given an integer array nums, return the largest perimeter of a triangle with a non-zero area, formed from three of these lengths.
If it is impossible to form any triangle of a non-zero area, return 0. A triangle is valid if the sum of any two sides is greater than the third side.

Input: nums = [2,1,2] Output: 5

Explanation:
You can form a triangle with sides 2, 2, and 1. The perimeter is 5.


triangle is valid only if a+b>c
so we sort array first so laregest element comes at end and we can check 

and start the loop from end so we have largest element in i and will get largest perimeter first we don't need to traverse further 
-> so it will reduce time
if sum of before two element (arr[i-2]+arr[i-1]) is greater then arr[i] largest element (arr[i-2]+arr[i-1]>arr[i])