Interval List Intersections
Given two lists of closed intervals, each list of intervals is pairwise disjoint and in sorted order.
Return the intersection of these two interval lists. A closed interval [a, b] (with a <= b) denotes the set of real numbers x with a <= x <= b.
The intersection of two closed intervals is a set of real numbers that are either empty or represented as a closed interval. For example, the intersection of [1, 3] and [2, 4] is [2, 3].

Input:
firstList = [[0,2],[5,10],[13,23],[24,25]], secondList = [[1,5],[8,12],[15,24],[25,26]]
Output: [[1,2],[5,5],[8,10],[15,23],[24,24],[25,25]]

Explanation: The intersections are [1,2] from [0,2] and [1,5], [5,5] from [5,10] and [1,5], [8,10] from [5,10] and [8,12], [15,23]
 from [13,23] and [15,24], [24,24] from [24,25] and [15,24], and [25,25] from [24,25] and [25,26].

how to intersection
start = max(a1, b1)
end   = min(a2, b2)                         start    ends
A = [1,5] B = [2,6] where intersection [max(1,2), min(5,6)] → [2,5] if start<=end sure intersection push it 
why max of start because it will give start of intersection and why min of end so it will give end of intersection

if compare two list but solve problem in log(n) how to do use while loop an make two pointer i and j use i
 for 1st array j for 2nd array, move i and j according and compare and push intersection

 consition if i<arr.length and increase i++ when work done 
 The interval that ends first is finished — it can’t form new intersections. so if if--> (A[i][1] < B[j][1]) i++; else j++



